-- ============================================================================
-- Bazar — Миграция 0007: ребрендинг ING MARKET → Bazar
-- Меняет название сайта, уже сохранённое в site_settings при первом запуске
-- Модуля 0. Внутреннее имя проекта/пакета (ing-market) не меняется — это не
-- бренд, а просто путь/идентификатор.
-- ============================================================================

update public.site_settings
set value = '{"text": "Bazar"}'::jsonb
where key = 'site_name';
